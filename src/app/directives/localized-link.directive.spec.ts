import { LocalizedLinkDirective } from './localized-link.directive';

describe('LocalizedLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalizedLinkDirective();
    expect(directive).toBeTruthy();
  });
});
